"use client";

import { useEffect, useState } from "react";
import { Plus, Pencil } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Skeleton } from "@/components/ui/skeleton";
import { Switch } from "@/components/ui/switch";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";

type Category = { id: number; name: string; slug: string; description?: string | null; sortOrder?: number; isActive?: boolean };
export function CategoryPage({ resource, title, description }: { resource: "post-categories" | "guru-categories"; title: string; description: string }) {
  const [items, setItems] = useState<Category[] | null>(null); const [error, setError] = useState(""); const [open, setOpen] = useState(false); const [editing, setEditing] = useState<Category | null>(null); const [form, setForm] = useState({ name: "", slug: "", description: "", sortOrder: 0, isActive: true });
  async function load() { try { const res = await fetch(`/api/${resource}`); const data = await res.json(); if (!res.ok || !data.success) throw new Error(data.error?.message ?? "Data tidak dapat dimuat."); setItems(data.data); } catch (e) { setError(e instanceof Error ? e.message : "Data tidak dapat dimuat."); } }
  useEffect(() => { void load(); }, [resource]);
  function start(item?: Category) { setEditing(item ?? null); setForm(item ? { name: item.name, slug: item.slug, description: item.description ?? "", sortOrder: item.sortOrder ?? 0, isActive: item.isActive ?? true } : { name: "", slug: "", description: "", sortOrder: 0, isActive: true }); setOpen(true); }
  async function save() { const response = await fetch(editing ? `/api/${resource}/${editing.id}` : `/api/${resource}`, { method: editing ? "PUT" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const result = await response.json(); if (!response.ok || !result.success) return setError(result.error?.message ?? "Perubahan gagal."); setOpen(false); await load(); }
  return <div className="mx-auto max-w-6xl space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs font-semibold text-slate-400">Admin / {title}</p><h1 className="text-2xl font-bold">{title}</h1><p className="mt-2 text-sm text-slate-500">{description}</p></div><Button className="bg-[#1D4F98] hover:bg-[#0B3477]" onClick={() => start()}><Plus />Tambah kategori</Button></div>{error && <p role="alert" className="text-sm text-red-700">{error}</p>}{items === null ? <Skeleton className="h-64 w-full" /> : <Card><CardHeader><CardTitle className="text-sm">{items?.length ?? 0} kategori</CardTitle></CardHeader><CardContent className="p-0"><Table><TableHeader><TableRow><TableHead>Nama</TableHead><TableHead>Slug</TableHead><TableHead>Urutan</TableHead><TableHead>Status</TableHead><TableHead className="w-24" /></TableRow></TableHeader><TableBody>{items?.map((item) => <TableRow key={item.id}><TableCell className="font-semibold">{item.name}</TableCell><TableCell className="text-slate-500">{item.slug}</TableCell><TableCell>{item.sortOrder ?? 0}</TableCell><TableCell><Switch checked={item.isActive} onCheckedChange={async (value) => { await fetch(`/api/${resource}/${item.id}`, { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...item, isActive: value }) }); await load(); }} /></TableCell><TableCell><Button variant="ghost" size="icon" onClick={() => start(item)} aria-label="Edit kategori"><Pencil /></Button></TableCell></TableRow>)}</TableBody></Table></CardContent></Card>}<div className="grid gap-3 md:hidden">{items?.map((item) => <Card key={item.id}><CardContent className="flex items-center justify-between p-4"><div><p className="font-semibold">{item.name}</p><p className="text-xs text-slate-500">{item.slug}</p></div><Button variant="outline" size="sm" onClick={() => start(item)}><Pencil />Edit</Button></CardContent></Card>)}</div><Dialog open={open} onOpenChange={setOpen}><DialogContent><DialogHeader><DialogTitle>{editing ? "Edit kategori" : "Tambah kategori"}</DialogTitle></DialogHeader><div className="space-y-4"><div className="space-y-2"><Label>Nama</Label><Input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></div><div className="space-y-2"><Label>Slug</Label><Input value={form.slug} onChange={(e) => setForm({ ...form, slug: e.target.value })} /></div>{resource === "post-categories" && <div className="space-y-2"><Label>Deskripsi</Label><Input value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} /></div>}<div className="flex items-center justify-between"><Label>Aktif</Label><Switch checked={form.isActive} onCheckedChange={(value) => setForm({ ...form, isActive: value })} /></div></div><DialogFooter><Button variant="outline" onClick={() => setOpen(false)}>Batal</Button><Button onClick={() => void save()} className="bg-[#1D4F98]">Simpan kategori</Button></DialogFooter></DialogContent></Dialog></div>;
}
