# Condition Grid Reference

Reference supplied by the user for Home / Highlight Prestasi.

Applied traits:

- Conditional 12-column asymmetric grid.
- Image cards with black title labels and circular diagonal-arrow links.
- In-view reveal animation using `motion/react`.
- Responsive single-column mobile layout.

Current implementation uses mock data. Planned CMS source: published `posts` where `type = "prestasi"`. Grid span currently derives from mock `level`; the post schema needs equivalent achievement-scale metadata before CMS integration.
